import 'package:flutter/material.dart';

const kTileHeight = 50.0;
const inProgressColor = Color(0xFF388E3C);
const todoColor = Color(0xffd1d2d7);

enum Pages {
  deliveryTime,
  addAddress,
  summary,
}